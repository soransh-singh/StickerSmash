import Common from "@/assets/css/common";
import { StyleSheet, Text, View } from "react-native";

export default function About() {
  return (
    <View style={[Common.container]}>
      <Text style={[Common.text]}>About Screen</Text>
    </View>
  );
}
